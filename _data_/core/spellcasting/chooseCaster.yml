type: Folder
hidden: true
children:
  - type: Effect
    target: attributes.spellcasting.save
    operation: set
    value: $(8 + attributes.prof + Math.max(stats.wis.mod, stats.cha.mod, stats.int.mod))
  - type: Effect
    target: attributes.spellcasting.attack
    operation: set
    value: $(attributes.prof + Math.max(stats.wis.mod, stats.cha.mod, stats.int.mod))
id: chooseCaster
name: Choose Caster
